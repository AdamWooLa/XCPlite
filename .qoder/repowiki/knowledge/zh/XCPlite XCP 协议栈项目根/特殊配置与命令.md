通过 `cmake -B build -S . -DXCPLITE_CONFIGURATION=<config>` 选择目标配置，再配合 `XCPLITE_BUILD_EXAMPLES=ON`、`XCPLITE_BUILD_TESTS=ON`、`XCPLITE_BUILD_TOOLS=ON`、`XCPLITE_BUILD_RUST_TOOLS=ON`、`XCPLITE_BUILD_BPF_DEMO=ON` 控制子模块构建；默认安装前缀为 `<build>/install`，可通过 `cmake --install` 安装到 `CMAKE_INSTALL_PREFIX`。