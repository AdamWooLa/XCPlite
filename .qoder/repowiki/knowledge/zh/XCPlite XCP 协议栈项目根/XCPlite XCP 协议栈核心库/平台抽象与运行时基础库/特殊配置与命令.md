Linux 构建必须在编译器命令行传入 `-D_GNU_SOURCE`（或通过 CMake target `xcplite::xcplite` 自动传播）；FreeRTOS POSIX 模拟器需额外 `-D_FREE_RTOS -DFREE_RTOS_POSIX_SIM`；可选 `OPTION_ATOMIC_EMULATION` 在 Windows 上启用 64 位原子模拟；STM32 FreeRTOS 路径默认将队列头部放入 `.dtcm`、数据区放入 `.noncacheable` 段。