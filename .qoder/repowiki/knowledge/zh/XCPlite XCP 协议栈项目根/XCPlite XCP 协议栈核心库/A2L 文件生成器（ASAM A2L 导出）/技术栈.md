纯 C 实现，使用 stdio 流式写入 A2L；通过 `OPTION_SHM_MODE`、`XCP_ENABLE_CALSEG_LIST`、`XCP_ENABLE_CAL_PAGE`、`XCP_ENABLE_DAQ_EVENT_LIST`、`XCPTL_ENABLE_MULTICAST` 等宏条件编译不同 A2L 片段；以太网传输层通过 `sockets.h` 的 `socketGetLocalAddr` 探测本机 IP。