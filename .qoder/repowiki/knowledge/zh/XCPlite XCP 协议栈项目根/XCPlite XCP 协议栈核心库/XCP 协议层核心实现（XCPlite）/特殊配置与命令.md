构建前需在 `xcplib_cfg.h` 中定义 `OPTION_DAQ_EVENT_COUNT`、`OPTION_QUEUE_32|64`、`OPTION_MTU` 等选项；`xcp_cfg.h` 强制要求 `CLOCK_TICKS_PER_S` 已定义且能无舍入表示纳秒分辨率；`xcptl_cfg.h` 对 UDP_RAW 与 TCP/UDP 互斥、RAW 必须配合 `OPTION_QUEUE_32` 有编译期 `#error` 检查。