构建时必须定义 OPTION_ENABLE_TCP / OPTION_ENABLE_UDP / OPTION_ENABLE_UDP_RAW 三者之一（否则编译报错）；启用 OPTION_ENABLE_UDP_RAW 时需同时定义 OPTION_QUEUE_32（禁用 64 位队列的 vectored send 路径），并在 socket_raw_hal.h 中提供 Linux AF_PACKET 或自定义 HAL 后端。