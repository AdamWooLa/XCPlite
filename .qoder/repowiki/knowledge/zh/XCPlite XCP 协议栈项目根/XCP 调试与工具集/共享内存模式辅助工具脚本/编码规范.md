- 每个脚本职责单一，仅封装对 `build/shmtool` 的特定子命令调用（status/finalize/clean），保持薄包装风格。
- 清理类脚本在调用外部工具后追加删除生成产物（如 `*.a2l`、`*.bin`）或锁文件，确保环境完全恢复。
- `shm_cleanup.sh` 采用“优先使用预构建工具，失败时回退到临时编译 C 片段”的双路径策略，避免引入 Python 等额外依赖。