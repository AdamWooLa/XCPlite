构建相关命令集中在 `docs/BUILDING.md`，核心入口为根目录 `./build.sh [debug|release] [configuration] [target] [options]`，支持 `default/no_a2l/ptp/shm/rtos/raw` 六种互斥配置，每种配置需独立构建目录；可通过 `-DXCPLITE_CFG_OVERRIDE=...` 注入应用级配置头文件，或通过 `examples/fetchcontent_example/` 以 FetchContent 方式消费 xcplite。