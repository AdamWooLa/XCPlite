- 每个主题以独立 Markdown 文件呈现，并通过 `docs/README.md` 统一索引归类（Getting Started / API Reference / Advanced Topics）。
- 构建说明采用表格形式列出配置、选项与目标之间的映射关系，便于快速查阅不同配置的可用功能。
- 所有命令行示例同时给出 `build.sh` 快捷用法与等价 `cmake -B ... -S .` 原生调用，保证两种构建路径均可复现。