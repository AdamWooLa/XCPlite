C/C++ 示例 + Bash 编排；依赖外部 CLI 工具 `xcpclient`（cargo install）、`bintool`（cargo install tools/bintool）、`a2ltool`；以太网测试使用 POSIX sockets（`sys/socket.h`）与 Linux 特定 `fork/sigaction`；可选 PTP 时钟路径通过 `OPTION_ENABLE_PTP` 编译开关启用 linuxptp `pmc` 命令解析。