运行全部示例：`./test.sh [clean] [example_name]`，需预先 CMake 构建到 `build/` 目录且 PATH 中存在 `bintool`、`a2ltool`、`xcpclient`；运行 bintool 端到端测试：`./test_bintool.sh [demo_name]`，要求本地可 ssh 登录目标主机（默认 `rainer@192.168.0.206`），并在目标上执行 `./build.sh Debug`；`eth_transport_test` 可在非 Windows/FreeRTOS 平台单独编译运行以触发 fork 子进程测试。